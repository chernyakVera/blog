/** Класс для работы с главной страницей */

public function main()

        /** достаем все статьи из таблицы БД `articles`
         * благодаря статическому методу findAll() класса Article;
         */


        /** отправляем данные в метод renderHtml класса View 
         * (которые потом отправятся в шаблон главной страницы),
         * которые будут располагаться на главной странице main 
         * (имя шаблона и массив со статьями,
         * который получили в переменную $articles ранее)
         */