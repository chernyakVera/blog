/** Класс для отправки email-ов пользователям */

public static function send(
        User $receiver,
        string $subject,
        string $templateName,
        array $templateVars = []
    ): void


        /* Импортируем переменные из массива в текущую таблицу символов.
        Работает только с ассоциативными массивами.
        Из вида ['key' => value] будет вид 'key' = value. */

        /* Включение буферизации вывода.
        Эта функция включает буферизацию вывода. Если буферизация
        вывода активна, никакой вывод скрипта не отправляется
        (кроме заголовков), а сохраняется во внутреннем буфере. */

        /* Возвращаем содержимое буфера вывода в прееменную $body */

        /*  Очистить (стереть) буфер вывода и отключить буферизацию вывода */