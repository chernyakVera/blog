Сервис, который будет работать с пользовательскими сессиями через Cookie. 


public static function createToken(User $user): void
/** Метод для создания токена для последующей отправки cookie. */





public static function getUserByToken(): ?User
/** Статический метод, кот. получает cookie с сайта,
     * проверяет их на пустоту, и далее запрашивает из БД пользователя
     * с id из cookie. Результатом метода будет или null, или строка
     * из БД.
     */

	/* Запрашиваем cookie с сайта, которые до этого отправили
        и проверяем на пустоту. */

	/* Получаем значения полученных cookie в 2 переменные:
        $userId и $authToken */

	/* Далее запрашиваем и получаем строку из БД с $userId,
        который получили ранее. И если она не пустая, т.е. такой пользователь
        был ранее зарегистрирован, то все хорошо, и просто возвращаем
        строку с этими данными(строкой из БД). */


	/* Для успешной авторизации токены также должны совпадать. */
















