/** Класс, который позволит нам создавать новые коды активации для
 * пользователей (и следовательно новые строки в таблице БД),
 * а также проверять код активации для конкретного пользователя.
 */



    /* Задали константу для данного класса - имя таблицы БД,
    для работы с которой и предназначен этот класс. */





public static function createActivationCode(User $user): string
/* Статический метод, принимающий только объект $user класса User
    (думаю, он приходит сюда из метода signUp() класса User) и
    возвращающий строку с кодом $code.
    В процессе выполнения создает новую строку в
    таблице БД `users_activation_cods` */


        /* Тут генерируем случайную последовательность символов с
        поомщью функ. random_bytes() получаем строку случайных бинарных
        символов всего длиной 16 байт и с помощью функ. bin2hex() получаем
        шестнадцатеричное представление строки, полученной ранее. */


        /* Переменная с запросом на добавление данных согласно
        синтаксису SQL:
        INSERT INTO table_name(константа данного класса)(column1, column2)
        VALUES(:param1, :param2) */

        /* В аргументы записываем sql запрос и параметры запроса,
        т.е. вида [:param1 => value1] для защиты от SQL-инъекций.
        Странно, что не прописываем последний аргумент - имя класса */

        /* Возвращаем значение кода $code, т.к. оно нам понадобится
        для работы с другими методами в будущем. */





public static function checkActivationCode(User $user, string $code): bool
/** Статический метод для проверки наличия кода $code
     * у пользователя $user(это массив явл. объектом класса User) в таблице 
       БД `users_activation_cods`. */

        /* Запрос на выборку вида:
        SELECT * FROM table_name(запрашиваем константу данного класса)
        WHERE column1(user_id) = :param1 AND column2(code) = :param2*/


        /* Тут говорим вернуть результат $result (это будет массив) запроса,
        если он НЕ ПУСТОЙ, т.е. переданный ранее user_id и
        code уже СУЩЕСТВУЮТ в таблице БД. */




public static function deleteActivationCode(int $userId)
 /* Метод для удаления всей строки таблицы `users_activation_cods`
    по userId пользователя. */

























