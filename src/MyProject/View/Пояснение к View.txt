Класс, отвечающий за V в шаблоне проектирования MVC.
Отображение, то, что видит пользователь.


public function renderHtml(string $templateName, array $vars = [], int $code = 200)
/** имя шаблона и массив получаем из MainController метод main() */


        /** функ., которая получает или устанавливает код ответа HTTP
         * В нашем случае, если страница существует (ранее была описана и создана в принципе),
         * то установится код ответа по умолчанию (200 - со страницей все хорошо)
         * и в панели разработчика в браузере увидим код 200,
         * а если ее не существует, то передаем и потом устанавливаем значение кода 404,
         * что значит, что страницы не существует и увидим этот код в панели разработчика
         */

        /** новые переменные из массива отправляются в указанный шаблон
         * ($this->templatesPath . '/' . $templateName) по пути проходя
         * через буфер
         */


        /** включили output buffering (буферизацию) */

	/** здесь мы подключаем  текущий экземпляр класса View,
         * берем у него путь до шаблона (он придет из MainController
         * или ArticleController из конструктора, т.к. там создаем
         * объект класса View и передаем как аргумент путь до наблона)
         * и склеиваем с '/' и именем шаблона
        */

        /** функ. возращает содержимое буфера в переменную $buffer */

	/** стираем данные из буфера и выключаем его */


        /** без распечатывания этой переменной не увидим блог,
         * т.к. все данные сейчас оказались в ней
         */